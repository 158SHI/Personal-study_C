#include<stdio.h>
int main(void)
{
    int i = 1;
    int y;
    while (i<=100)
    {
        y = i % 2;
        if (y == 1)
        {
            printf("%d ", i);
        }
        i++;
    }
    return 0;
}
